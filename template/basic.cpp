#include <bits/stdc++.h>
#define endl '\n'
using namespace std;

void fast_IO() // fold it at start
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
}

void solution(const int& T)
{
    for (int t = 0; t < T; t++)
    {

    }
}

int main(void)
{
    fast_IO();

    int T;
    cin >> T;

    solution(T);

    return 0;
}