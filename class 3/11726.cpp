#include <bits/stdc++.h>

using namespace std;

void solution(int n)
{
    vector<int> dp(1000);

    for (int i = )
}

void fast_IO()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
}

int main(void)
{
    fast_IO();

    int n;
    cin >> n;

    solution(n);

    return 0;
}